<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".LoginActivity" >

    <TextView android:id="@+id/login_name"
        android:layout_width="80dp"
        android:layout_height="@dimen/login_height"
        android:text="@string/login_name" />
    
    <EditText android:id="@+id/login_edt_name"
        android:layout_width="180dp"
        android:layout_height="@dimen/login_height"
        android:layout_toRightOf="@id/login_name"
        android:inputType="text"/>
    
    <TextView android:id="@+id/login_pwd"
        android:layout_width="80dp"
        android:layout_height="@dimen/login_height"
        android:layout_below="@id/login_name"
        android:text="@string/login_pwd"/>
    
    <EditText android:id="@+id/login_edt_pwd"
        android:layout_width="180dp"
        android:layout_height="@dimen/login_height"
        android:layout_toRightOf="@id/login_pwd"
        android:layout_alignTop="@id/login_pwd"
        android:inputType="textPassword"/>
    
    <Button android:id="@+id/login_btn_canel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/login_canel"
        android:layout_below="@id/login_edt_pwd"
        android:layout_alignParentRight="true"/>
    
    <Button android:id="@+id/login_btn_ok"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/login_ok"
        android:layout_alignTop="@id/login_btn_canel"
        android:layout_toLeftOf="@id/login_btn_canel"/>

</RelativeLayout>
